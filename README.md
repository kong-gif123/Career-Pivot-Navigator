# Career-Pivot-Navigator
An Maven + JDK 17 desktop app using Strategy Pattern (matchers &amp; recommenders), ApiClient isolation, retries/backoff, caching, and graceful fallback to a local catalog when API fails.
